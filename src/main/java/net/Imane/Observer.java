package net.Imane;

public interface Observer {
    void updateObservable(int state);// pour la methode push
   // void update(Observable observable);//pour la method pop en utilise pas le paramettre state
}
